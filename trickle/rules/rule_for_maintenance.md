When updating the project
- Check if it is necessary to update the `README.md` content in `trickle/notes`.
- Ensure all new assets are recorded in `trickle/assets`.
- Keep the visual consistency with the defined theme variables in `index.html`.